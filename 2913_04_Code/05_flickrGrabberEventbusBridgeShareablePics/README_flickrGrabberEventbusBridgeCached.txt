Uses shared data for caching flickr queries among multiple instances of webclient verticle.

To run it, type:

	vertx run webserver.js -instances 2

Point your browser to http://localhost:8080/ search forpictures. Click on images to share them. 


